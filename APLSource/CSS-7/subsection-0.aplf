 subsection←{
     s←⊂{
         s←⊂'.subsection'
         ⍝ s,←⊂'margin-top' '.75rem'
         s
     }⍵
     s,←⊂{
         s←⊂'.subsection>summary'
         s,←⊂'font-family' 'Arial'
         s,←⊂'font-size' '1.25rem'
         s,←⊂'font-weight' 'bold'
         s

     }⍵
     s,←⊂{s←⊂'.subsectioncontent'
         s,←⊂'padding' '1rem 0 1rem 0'
         s}⍵
     s
 }

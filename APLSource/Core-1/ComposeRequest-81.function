 ComposeRequest←{
     ⍝ ⍺ ←→ Client
     ⍝ ⍵ ←→ Request
     nl←⎕UCS 13 10
     t←⍵.Method
     t,←' ',EncodeBlanks ⍵.URI
     t,←' ',⍺.Version{⍵≡'':⍺ ⋄ ⍵}⍵.Version
     ⍵.CompressedContent←(EncodeGzip⍣⍵.Compress)⍵.Content
     _←⍺ DefaultRequestHeaders ⍵
     h←ComposeHeaders ⍵
     rh←t,h,nl,nl
     rb←⍵.CompressedContent
     rh rb
 }

 DisplayCache←{
     s←⍵
     c←s.Cache
     v←c.CacheMatrix[;c.RequestTarget c.Hit c.ResponseTime c.Invalid c.FreshnessLifetime c.InitialAge]
     'RequestTarget' 'Hit' 'ResponseTime' 'Invalid' 'FreshnessLifetime' 'InitialAge'⍪v
 }

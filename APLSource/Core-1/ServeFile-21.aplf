 ServeFile←{
     ⍝ ⍺ ←→ Optional 404 Response
     ⍝ ⍵ ←→ Request
     ⍺←⍵ Respond 404 1
     e n←DefaultRequestFile ⍵
     ~e:⍺
     r←NewResponse ⍵
     r.UTF8←0
     r.Content←ReadBytes n
     r.ContentType←FilenameToContentType n
     r
 }

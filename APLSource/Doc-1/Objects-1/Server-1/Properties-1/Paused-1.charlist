[DATA]:index=''

# Paused

Applies to:{.prefix}

Server{.info}

This is a read-only property of type boolean.

It is set only when Rumba is running as a Windows Service. Such a service can be paused, and this property reflects this.

When the service is paused Rumba will respond to any new request with 503 "Service unavailable".

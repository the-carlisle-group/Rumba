[DATA]:index=''

# Secure

Applies to:{.prefix}

Server{.info}

Boolean that defaults to 0. Set this to enforce secure connections (HTTPS).

Note that if this is 1 then →[PublicCertFile], →[PrivateKeyFile] and →[CertType] must be set;
the older →[X509] property is now deprecated.

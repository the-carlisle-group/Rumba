# HTTP Status Code Registry

HTTP status codes and default reason phrases are stored in the Core.StatusCodeRegistry function. It
is provided here for reference only. There is no need to call this function directly.

⍎⍎#.Rumba.Doc.GetFunctionBody 'StatusCodeRegistry'⍎⍎


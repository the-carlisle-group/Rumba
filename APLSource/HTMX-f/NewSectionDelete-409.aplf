 NewSectionDelete←{
     ⍝ ⍺ ←→ Optional Parent
     ⍝ ⍵ ←→ Caption URL
     ⍺←0
     c u←⍵
     s←⍺ A.New c'unsafe'
     f←s.Form
     b←f NewCommand'Delete'
     b.hx_delete←u
     ⍝ b.hx_swap←'none'   ⍝ Needed?
     s
 }
